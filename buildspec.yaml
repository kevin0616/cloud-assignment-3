version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
  build:
    commands:
      - echo "Packaging CloudFormation template..."
      - aws cloudformation package --template-file template.yaml --s3-bucket cc-artifact-s3-bucket --output-template-file packaged.yaml

artifacts:
  files:
    - packaged.yaml